# Normalize line endings so the repo is identical on Windows, macOS and Linux.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.mp3 binary
*.mp4 binary
*.svg binary
*.rnnn binary
